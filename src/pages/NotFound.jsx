import React from 'react';

const styles = {
  textAlign: 'center',
  fontSize: '2rem',
  paddingTop: '2rem',
  paddingBottom: '1rem'
};

export default () => (
  <div style={styles}>
    Sorry, did not find what your are looking for.
  </div>
);
